Sources for the plugin `plugin.playfab.server`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.playfab.server" = {
            publisherId = "com.playfab",
        },
    },
}
```
